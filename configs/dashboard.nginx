server {
	listen 80; #Lắng nghe cổng 80
	server_name localhost; #tên miền

	root /var/www/html; #Phải khớp tới chỗ script monitor.sh
	index index.html;

	location / {
		try_files $uri $uri/ =404;
	}
}
